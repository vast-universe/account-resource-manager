{
  "name": "account-resource-manager",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @account-resource-manager/web",
    "build:web": "npm run build --workspace @account-resource-manager/web",
    "lint:web": "npm run lint --workspace @account-resource-manager/web"
  }
}
